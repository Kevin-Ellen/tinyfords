{
  "name": "hotwheels-v1",
  "version": "1.0.0",
  "description": "Starting HotWheels v1",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "{{ authors }}",
  "license": "MIT",
  "dependencies": {
    "wrangler": "^2.20.1"
  }
}
